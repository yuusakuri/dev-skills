# コントリビュート

このリポジトリは選定です。通常の貢献は[`catalog.json`](catalog.json)への編集であって、新しいスキルの追加ではありません。

## 選定にスキルを加える

1. 保守されているリポジトリで公開されている既存のスキルで、寛容なライセンスが明示されていることを確認します。
2. 工程の空白を埋めるか、既存のものを置き換えるに足るだけ明確に上回ることを確認します。
   同じ依頼に2つのスキルが該当する状態は危険です。エージェントがどちらを選ぶか予測できなくなります。
3. `catalog.json`に項目を加えます。上流の`repo`、40文字のコミットSHAを`ref`、スキルのディレクトリへの`path`、導入する`plugin`、1行の`role`を書きます。
4. `development-lifecycle/SKILL.md`の工程の対応表に加えます。
   カタログの項目が割り当てられていないと、検証がビルドを落とします。
5. `docs/reference/catalog.md`と`NOTICE.md`を合わせて更新し、下記の検査を実行します。

## ここに新しいスキルを書く場合の基準

選ぶことを、書くことより優先します。ここに置くより、上流へ貢献することを優先します。

このリポジトリに新しいスキルを置くには、次のすべてを満たす必要があります。

1. 既存の公開スキルが扱っていないこと。
   まとめリポジトリ、`catalog.json`の取得元、Claude Codeのプラグイン一覧を調べてください。
2. 技術構成に依存しないこと。特定のフレームワーク、クラウド、ベンダーを要求しないこと。
3. 起動条件が明確で、選定済みのスキルと衝突しないこと。
4. 文書ではなく判断を記していること。

`development-lifecycle`ルーターがここに置かれている唯一のスキルです。特定の選定に対する割り当ての地図は、上流には置けないためです。

## 検査

```bash
python3 scripts/verify-catalog.py   # 選定した各refが固定したコミットで解決する
python3 scripts/validate-skills.py  # 仕様への適合と、ルーターが全項目を網羅していること
./scripts/check-upstream.sh         # 固定先が不変のコミットであり、タグとも一致する
```

3つともCIで実行し、週次でも実行しています。上流のずれが、静かに誤った地図としてではなく、失敗するビルドとして現れるようにするためです。

## カタログと文書を揃えておく

`catalog.json`が唯一の情報源です。

`docs/reference/catalog.md`と`NOTICE.md`は読者向けにそれを言い直したもので、手で管理しています。カタログへの編集は、両方を同じコミットで更新するまで終わっていません。

これを強制する仕組みはまだありません。`verify-catalog.py`は`catalog.json`の各項目が上流で解決するかを見るだけで、文章がカタログと一致しているかは見ていません。

## 固定先を更新する

`catalog.json`の`ref`を編集し、3つの検査をすべて実行します。

先に上流の変更履歴を読んでください。上流のスキル名は割り当ての表の一部なので、改名はルーターを壊します。`verify-catalog.py`はまさにそれを捕らえます。
